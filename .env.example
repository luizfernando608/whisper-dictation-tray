# (Opcional / legado) As chaves de API agora são configuradas pela tela de
# Configurações do app e guardadas com segurança no Gerenciador de Credenciais
# do Windows. Este arquivo só é lido como fallback, caso você prefira variáveis
# de ambiente. Nunca faça commit de chaves reais.
GROQ_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
